# Generated by jeweler
# DO NOT EDIT THIS FILE
# Instead, edit Jeweler::Tasks in Rakefile, and run `rake gemspec`
# -*- encoding: utf-8 -*-

Gem::Specification.new do |s|
  s.name = %q{yaml_db}
  s.version = "1.0.0"

  s.required_rubygems_version = Gem::Requirement.new(">= 0") if s.respond_to? :required_rubygems_version=
  s.authors = ["Orion Henry", "Adam Wiggins"]
  s.date = %q{2009-08-19}
  s.description = %q{Data is written and read from db/data.yml, or to file of your choosing.  Records are written in batches making this perfect for dumping very large databases or on systems with limited memory resources, such as shared hosting.  A modest 250 records are read at a time though this can be customized by passing an argument to the rake tasks.}
  s.email = %q{kjvarga@gmail.com}
  s.extra_rdoc_files = [
    "README.rdoc"
  ]
  s.files = [
    "README.rdoc",
     "Rakefile",
     "VERSION",
     "init.rb",
     "lib/yaml_db.rb",
     "spec/base.rb",
     "spec/yaml_dump_spec.rb",
     "spec/yaml_load_spec.rb",
     "spec/yaml_utils_spec.rb",
     "tasks/yaml_db_tasks.rake"
  ]
  s.homepage = %q{http://github.com/kjvarga/yaml_db}
  s.rdoc_options = ["--charset=UTF-8"]
  s.require_paths = ["lib"]
  s.rubygems_version = %q{1.3.4}
  s.summary = %q{Dumps and loads database contents in a database-agnostic YAML format.}
  s.test_files = [
    "spec/base.rb",
     "spec/yaml_dump_spec.rb",
     "spec/yaml_load_spec.rb",
     "spec/yaml_utils_spec.rb"
  ]

  if s.respond_to? :specification_version then
    current_version = Gem::Specification::CURRENT_SPECIFICATION_VERSION
    s.specification_version = 3

    if Gem::Version.new(Gem::RubyGemsVersion) >= Gem::Version.new('1.2.0') then
      s.add_runtime_dependency(%q<activerecord>, [">= 0"])
    else
      s.add_dependency(%q<activerecord>, [">= 0"])
    end
  else
    s.add_dependency(%q<activerecord>, [">= 0"])
  end
end
